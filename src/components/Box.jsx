import styled from '@emotion/styled';
import {
    color,
    layout,
    space,
    flexbox,
    border,
    shadow,
    position,
} from 'styled-system';

export const Box = styled.div`
    ${color}
    ${space}
  ${layout}
  ${flexbox}
  ${border}
  ${shadow}
  ${position}
`;
